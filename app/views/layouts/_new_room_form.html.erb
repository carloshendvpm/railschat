<%= form_with(model: @room, remote: true, class: 'flex gap-2') do |f| %>
  <%= f.text_field :name, class: 'input input-bordered input-sm w-full max-w-xs', autocomplete: 'off'%>
  <%= f.submit data: {disable_with:false}, class: 'btn btn-sm' %>
<% end %>